mixin form(data)
	-
		data = data || {};
		data.attributes = data.attributes || {};
		data.tagName = data.tagName || data.attributes.action ? 'form' : 'div'
	#{data.tagName}.form(
		class=data.class
		class=mods('form', data.mods)
		)&attributes(data.attributes)
		block
