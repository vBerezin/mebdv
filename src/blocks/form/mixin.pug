mixin form(data)
	- data = data || {};
	- tagName = data.attributes && data.attributes.action ? 'form' : 'div';
	#{tagName}.form(
		class=data.class
		class=mods('form', data.mods)
	)&attributes(data.attributes)
		block
